/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    nextConfig,
    images: {
        domains: ['images.unsplash.com'], // Add any other domains for external images
    },
}
